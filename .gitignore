tsconfig.tsbuildinfo
dist
node_modules
package-lock.json
.claude/
releases/
# Environment variables (secrets — never commit)
.env
.env.local
.env.*.local
# .env.test is intentionally committed — no secrets, just clears Supabase vars for CI/tests
